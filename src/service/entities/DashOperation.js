export default class DashOperation{
    constructor(operation, emise , apure_non_valide , valide){
     this.operation=operation;
     this.apure_non_valide=apure_non_valide;
     this.emise=emise;
     this.valide=valide;
    }
}